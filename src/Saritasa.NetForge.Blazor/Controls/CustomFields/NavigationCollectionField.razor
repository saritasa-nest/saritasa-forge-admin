@inherits CustomField

@typeparam T

<MudSelect @bind-SelectedValues="NavigationCollection"
           Variant="Variant.Outlined"
           MultiSelection="true">
    @foreach (var instance in NavigationInstances)
    {
        <MudSelectItem Value="instance" />
    }
</MudSelect>
