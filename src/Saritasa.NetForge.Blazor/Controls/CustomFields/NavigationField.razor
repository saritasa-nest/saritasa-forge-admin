@inherits CustomField

<MudSelect @bind-Value="PropertyValue" Variant="Variant.Outlined">
    @foreach (var instance in NavigationInstances)
    {
        <MudSelectItem Value="instance"></MudSelectItem>
    }
</MudSelect>
